[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edp_us5"
version = "0.4.0"
description = "EDP use case us5: curated and datamart layers for settlement."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["us5_module*"]
