[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edp_us1"
version = "0.4.0"
description = "EDP use case us1: curated and datamart layers for orders."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["us1_module*"]
